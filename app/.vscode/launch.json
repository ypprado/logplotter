{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Edge - Local File",
            "request": "launch",
            "type": "msedge",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
